* Look into various errors when doing step 2; these should be captured better and retried if necessary,
    We observe the following errors in 2.err, but none of them actually killed processing on this round:
        Fatal error. Exiting
        Fatal error: Unauthorized query.
* Investigate why caching fails to capture some datasets when redundant calls exist
* Parallelize step 4 (HAR)
* Have steps 2 and 4 write logs to logs/step2 and logs/step4, respectively
* parallelize demographics
